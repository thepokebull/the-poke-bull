# ── secrets / keys (NEVER commit) ──
.env
.env.local
.env.*.local
wallet.key
wallet.json
*.key
*.pem
*.keypair

# ── runtime state / logs ──
airdrop-state.json
dashboard/pulls.json
probe-*.mjs

# ── local test harness / captures (not distributed) ──
test-open.mjs
test-open-capture.json

# ── third-party brand assets (not distributed) ──
dashboard/public/*.webm
dashboard/public/*.png

# ── deps / os ──
node_modules
.DS_Store
npm-debug.log*

# local-only admin (never publish)
dashboard/local.js
lib/local.js
bot-control.json

# ── hardened 2026-07-12: timestamped backups slipped past *.key / .env ──
.env.*
*.env.bak*
wallet.key*
*.key.*
*.bak
*.bak.*
